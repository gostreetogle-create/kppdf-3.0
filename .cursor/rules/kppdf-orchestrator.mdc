---
description: ГЛАВНЫЙ ОРКЕСТРАТОР KPPDF — маршрутизация задач к специалистам
globs: **
alwaysApply: true
---

# Оркестратор KPPDF — маршрутизация задач

> **Ты — главный оркестратор.** Прочитай эту таблицу → выбери специалиста → действуй по его правилу.
> Детальные правила всех агентов: AGENTS.md
> Полный контекст: .opencode/AI_CONTEXT.md

## Таблица маршрутизации

| Задача | Специалист (правило Cursor / файл OpenCode) |
|--------|---------------------------------------------|
| UI, формы, таблицы, kp-* | `kppdf-ui.mdc` → .opencode/agents/ui-specialist.md + ui-manifest.md |
| Аудит UI | `kppdf-ui.mdc` → .opencode/agents/ui-qa.md → ui-auditor.md |
| Меню, разделы, навигация, IA | .opencode/agents/ux-architect.md |
| Архитектура FE, импорты, слои | `kppdf-architecture.mdc` → .opencode/agents/guardian.md |
| Backend (Express, Mongo, seed) | `kppdf-backend.mdc` → .opencode/agents/backend-specialist.md |
| API, shared/types, контракты | .opencode/agents/api-specialist.md |
| JWT, RBAC, Auth, guards | .opencode/agents/auth-specialist.md |
| EAV, BOM, сложная предметка | .opencode/agents/meta-architect.md |
| Дизайн-токены SCSS, CSS | .opencode/agents/design-system.md |
| Code review | .opencode/agents/reviewer.md |
| Деплой, CI/CD, nginx | `kppdf-deploy-synology.mdc` → deploy/synology/RUNBOOK.md |
| Сводка аудита | .opencode/agents/chief-architect.md |
| FreezeGuard / frozen-файлы | `.opencode/lock/FREEZE-RULES.md` + `kppdf-freezeguard.mdc` |
| Кодировки `.ps1` / `.sh` / Windows dev | `.opencode/rules/encoding-windows.md` + `kppdf-encoding-windows.mdc` |

## Протокол работы

1. Получил задачу → определи тип (UI/BE/архитектура/другое)
2. **Проверь FreezeGuard** — файл не frozen/locked (см. `.opencode/lock/INDEX.yaml`)
3. Если задача простая (1 файл, 10 строк) — сделай сам
4. Если задача сложная — используй таблицу, прочитай правило специалиста
5. После UI-работы: UI-specialist → UI-QA (Red Team) → UI-auditor
6. После любого изменения: проверь сборку (ng build / npm test)
7. После правки `*.ps1`: `npm run ps1:bom` + `npm run ps1:check` (см. encoding-windows.md)
8. ВАЖНО: никогда не читай все правила сразу — только то, что нужно под задачу

## Git commit (только AI, не пользователь)

Пользователь **не запускает** freeze-команды и **не коммитит** сам. Полный протокол: [`.opencode/lock/FREEZE-RULES.md`](.opencode/lock/FREEZE-RULES.md) → «Pre-commit протокол».

**Кратко — порядок перед каждым commit:**

1. `npm run lint` + `npm run build` + `ng test` (headless) + `backend/npm test`
2. Если менялись **frozen/locked** файлы (с approval) → `npm run freeze:update`
3. `npm run freeze:check` — **exit 0 обязателен**
4. `git commit` — включить обновлённые `.opencode/lock/modules/*.yaml` если был step 2
5. При hash mismatch **без approval** → не коммитить, не предлагать override пользователю

**Не предлагать** пользователю: `git commit`, `freeze:update`, `freeze:check`, `GIT_ALLOW_FREEZE=1`, `--no-verify`.

## Экономия токенов (ОБЯЗАТЕЛЬНО к исполнению)

- ✅ НЕ читай все агентов сразу — только того, кто нужен под задачу
- ✅ НЕ дублируй AGENTS.md в правилах — ссылайся на него
- ✅ НЕ загружай .opencode/rules/ целиком — только нужный файл
- ✅ НЕ держи opencode.json в контексте после прочтения
- ✅ Используй `globs` — правила грузятся только по совпадению с файлами
- ✅ НЕ читай все 17 агентов OpenCode — читай одного по таблице
- 🔴 НЕ делай лишних read_files — читай только релевантные файлы

---